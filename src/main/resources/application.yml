server:
  port: 8083

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/imform
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernage.SQL: debug
    org:
      hibernage:
        type: trace
